# Package Installation Guide

## ⚠️ Важные заметки об установке пакетов

### Production библиотеки (опциональные)

Некоторые production библиотеки требуют нативной компиляции и могут быть проблематичными на Windows:

#### 1. liboqs-node (PQC Crypto)

**Проблема:** Требует нативной компиляции и может не работать на Windows без дополнительных инструментов.

**Решение:**
- Используется упрощенная реализация по умолчанию (`pqc-crypto.ts`)
- Для production на Linux/Docker: `npm install liboqs-node`
- Для Windows: используйте WSL или Docker

**Установка (Linux/Docker):**
```bash
npm install liboqs-node
```

#### 2. rocksdb (Storage)

**Проблема:** Требует нативной компиляции, может быть проблематичным на Windows.

**Решение:**
- Используется файловая система по умолчанию (`storage-manager.ts`)
- Для production на Linux/Docker: `npm install rocksdb`
- Для Windows: используйте WSL или Docker

**Установка (Linux/Docker):**
```bash
npm install rocksdb
```

#### 3. @xenova/transformers (AI)

**Статус:** ✅ Работает на всех платформах

**Установка:**
```bash
npm install @xenova/transformers
```

**Примечание:** Это большая библиотека (~500MB), загрузка может занять время.

---

## 📦 Установка зависимостей

### Базовые зависимости (работают везде)

```bash
cd backend
npm install
```

Это установит:
- ✅ express, cors, dotenv
- ✅ ws (WebSocket)
- ✅ uuid, joi
- ✅ Все типы TypeScript

### Production библиотеки (Linux/Docker)

Если вы на Linux или в Docker контейнере:

```bash
# Установите build tools (Ubuntu/Debian)
sudo apt-get update
sudo apt-get install -y python3 make g++ libsnappy-dev libbz2-dev libzstd-dev liblz4-dev

# Установите production библиотеки
npm install liboqs-node rocksdb @xenova/transformers
```

### Для Windows (рекомендуется Docker)

Для Windows рекомендуется использовать Docker, чтобы избежать проблем с нативной компиляцией:

```bash
# Используйте Docker Compose (все уже настроено)
docker-compose up -d
```

Или установите в WSL:

```bash
# В WSL
sudo apt-get update
sudo apt-get install -y python3 make g++ libsnappy-dev libbz2-dev libzstd-dev liblz4-dev
npm install liboqs-node rocksdb @xenova/transformers
```

---

## ✅ Текущая конфигурация

### package.json (обновлено)

Production библиотеки **не включены** в dependencies по умолчанию, чтобы избежать проблем на Windows.

**Базовая установка работает везде:**
```bash
npm install
```

**Production библиотеки устанавливаются опционально:**
```bash
# Linux/Docker
npm install liboqs-node rocksdb @xenova/transformers

# Windows - используйте Docker или WSL
```

---

## 🔄 Fallback механизм

Все production версии автоматически используют fallback, если библиотеки не установлены:

1. **PQC Crypto:**
   - Если `liboqs-node` не установлен → использует `pqc-crypto.ts` (упрощенная версия)
   - Работает, но не quantum-resistant

2. **Storage:**
   - Если `rocksdb` не установлен → использует `storage-manager.ts` (файловая система)
   - Работает, но медленнее чем RocksDB

3. **AI Engine:**
   - Если `@xenova/transformers` не установлен → использует `ai-engine.ts` (mock)
   - Работает, но без реальных моделей

---

## 🐳 Docker (рекомендуется)

Docker решает все проблемы с нативной компиляцией:

```bash
# Build (включает все production библиотеки)
docker build -t presidium-backend:latest ./backend

# Run
docker-compose up -d
```

Dockerfile автоматически устанавливает все необходимые build tools и библиотеки.

---

## 🧪 Тестирование без production библиотек

Все тесты работают без production библиотек:

```bash
npm test
```

Fallback версии полностью функциональны для разработки и тестирования.

---

## 📝 Итоговая рекомендация

### Для разработки (Windows)
```bash
npm install  # Базовые зависимости
npm run build
npm start    # Использует fallback версии
```

### Для production (Linux/Docker)
```bash
npm install
npm install liboqs-node rocksdb @xenova/transformers
npm run build
npm start    # Использует production версии
```

### Для Docker (рекомендуется)
```bash
docker-compose up -d  # Все автоматически настроено
```

---

**Важно:** Все компоненты работают в режиме fallback, если production библиотеки не установлены. Это позволяет разрабатывать и тестировать на любой платформе без дополнительных зависимостей.

